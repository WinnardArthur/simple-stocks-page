/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['img.freepik.com', 'static.vecteezy.com', 'upload.wikimedia.org']
  }
}

module.exports = nextConfig
