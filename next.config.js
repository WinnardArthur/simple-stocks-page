/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['img.freepik.com', 'static.vecteezy.com', 'encrypted-tbn0.gstatic.com', 'upload.wikimedia.org']
  }
}

module.exports = nextConfig
